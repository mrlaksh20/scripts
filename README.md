``` usage : .\lazy.exe -u https://target.com/FUZZ -w .\SecLists\Discovery\Web-Content\common.txt -delay 200ms -t 100 -fs 6080 ```
-fs for filtering Size, -fc for status code, -t for concurrent threads,-dealy for dealy in milliseconsd for per request in concurrency.
